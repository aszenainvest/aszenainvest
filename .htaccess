<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  # Varsa gerçek dosya/klasörleri olduğu gibi sun
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
  # Diğer her şeyi index.html'e yönlendir
  RewriteRule . /index.html [L]
</IfModule>